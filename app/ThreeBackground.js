'use client';import{Canvas,useFrame}from'@react-three/fiber';import{useRef}from'react';import*as THREE from'three';const fragmentShader=`uniform float u_time;varying vec2 vUv;void main(){vec2 uv=vUv;float t=u_time*0.15;float wave=sin((uv.x*3.0)+t)*0.03+sin((uv.x*1.4)-(t*0.6))*0.02;float shade=smoothstep(0.4,0.47,uv.y+wave)-smoothstep(0.47,0.54,uv.y+wave);vec3 base=vec3(0.97,0.98,1.0);vec3 accent=vec3(0.80,0.86,1.0);vec3 color=mix(base,accent,shade*0.6);gl_FragColor=vec4(color,1.0);}`;const vertexShader=`varying vec2 vUv;void main(){vUv=uv;gl_Position=vec4(position,1.0);} `;function FluidStripe(){const ref=useRef();useFrame(({clock})=>{if(ref.current)ref.current.material.uniforms.u_time.value=clock.getElapsedTime();});return(<mesh ref={ref}><planeGeometry args={[2,2]}/><shaderMaterial uniforms={{u_time:{value:0}}} vertexShader={vertexShader} fragmentShader={fragmentShader}/></mesh>);}export default function ThreeBackground(){return(<div className='fixed inset-0 z-0 pointer-events-none bg-[#f9fafb]'><Canvas gl={{antialias:true}} dpr={[1,2]} orthographic camera={{zoom:200,position:[0,0,5]}}><FluidStripe/></Canvas></div>);}
