'use client';import{Canvas,useFrame}from'@react-three/fiber';import{useRef}from'react';import*as THREE from'three';const frag=`uniform float u_time;varying vec2 vUv;void main(){vec2 uv=vUv;float t=u_time*0.05;vec2 c1=vec2(.3,.3)+vec2(sin(t*.7)*.02,cos(t*.4)*.02);vec2 c2=vec2(.7,.6)+vec2(cos(t*.5)*.02,sin(t*.3)*.02);float d1=1.0-smoothstep(.0,.6,length(uv-c1));float d2=1.0-smoothstep(.0,.7,length(uv-c2));vec3 base=vec3(0.97,0.98,1.0);vec3 glow1=vec3(0.75,0.82,1.0);vec3 glow2=vec3(0.88,0.92,1.0);vec3 col=base;col=mix(col,glow1,d1*0.25);col=mix(col,glow2,d2*0.20);gl_FragColor=vec4(col,1.0);} `;const vert=`varying vec2 vUv;void main(){vUv=uv;gl_Position=vec4(position,1.);} `;function BG(){const r=useRef();useFrame(({clock})=>{if(r.current)r.current.material.uniforms.u_time.value=clock.getElapsedTime();});return(<mesh ref={r}><planeGeometry args={[2,2]}/><shaderMaterial uniforms={{u_time:{value:0}}} vertexShader={vert} fragmentShader={frag}/></mesh>);}export default function ThreeBackground(){return(<div className='fixed inset-0 z-0 pointer-events-none bg-[#f9fafb]'><Canvas gl={{antialias:true}} dpr={[1,2]} orthographic camera={{zoom:200,position:[0,0,5]}}><BG/></Canvas></div>);}
